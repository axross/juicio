source "https://rubygems.org"

# drives the Android preview-build and Firebase App Distribution publish
# lanes in fastlane/Fastfile. see docs/operations/preview-deployment.md.
gem "fastlane"

eval_gemfile("fastlane/Pluginfile")
