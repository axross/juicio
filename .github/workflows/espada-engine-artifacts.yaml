name: Espada Engine Artifacts

# Cross-compiles modules/espada-engine/lib/ for both platforms, generates its
# Nitro bindings, and opens one pull request committing all three resulting
# artifacts — see the plan behind this (issue #7) for why the binaries are
# committed rather than rebuilt on every ordinary build: producing the iOS
# one needs a macOS runner, which bills at roughly 10x a ubuntu-latest one
# ($0.062/min against $0.006/min — see docs/operations/preview-deployment.md),
# so this workflow, like the two preview pipelines, spends that cost only
# when a human explicitly asks for it.
#
# PRODUCING THIS MODULE'S COMMITTED ARTIFACTS HAPPENS ENTIRELY HERE. There is
# no local script that reproduces this on a maintainer's own machine — a
# developer who never touches modules/espada-engine/ keeps exactly today's
# workflow: no Rust toolchain, no NDK, no local build step. A maintainer who
# does need to iterate on the Rust or the Nitro spec still has
# `npm run nitrogen:espada-engine` locally (see package.json and
# merge-checks.yaml's own nitrogen_drift job); only the cross-compiles and
# the artifact-verification steps below live in this workflow alone.
#
# MANUAL DISPATCH ONLY, NAMING THE PULL REQUEST'S BASE BRANCH: no
# `pull_request`, `push`, or `schedule` trigger, matching this project's
# standing policy that anything spending macOS minutes runs only when a human
# dispatches it. This workflow always builds from whatever ref it is
# dispatched against (the same ref android-preview.yaml and ios-preview.yaml
# check out when dispatched, except those two take a pull request number and
# check out that pull request's head — this workflow has no pull request yet
# to check out, since producing one is its own job); `base_branch` only names
# what the pull request this workflow opens targets as its base, so the same
# workflow serves both "the source already landed on main" and "the source is
# still on a feature branch under review."
#
# FOUR JOBS: `build-android` (ubuntu-latest) cross-compiles for
# aarch64-linux-android, verifies the produced .so is 16 KB page aligned, and
# verifies its exported C ABI matches
# modules/espada-engine/lib/espada-engine/src/ffi.rs — failing the job, and
# never uploading an artifact, if either check does not pass; `build-ios`
# (macos-latest) builds the two Apple arm64 slices, verifies each slice's own
# exported C ABI against that same ffi.rs (a subset check, not an exact-set
# one — see that job's own comment), and assembles them into an
# .xcframework with `xcodebuild -create-xcframework` (`lipo` cannot merge
# them — both slices are arm64, which is exactly why this is an
# .xcframework and not a fat static library); `generate-bindings`
# (ubuntu-latest) runs Nitrogen against modules/espada-engine's `.nitro.ts`
# spec to produce its C++ bindings, registration, and per-platform
# autolinking files — it carries no `needs:` and runs concurrently with the
# two build jobs, not after them, because Nitrogen reads only the TypeScript
# spec and nitro.json and the Rust cross-compiles do not depend on its
# output in either direction (see that job's own comment before "fixing"
# this by adding a dependency); `open-pull-request` (ubuntu-latest) waits on
# all three, downloads their artifacts, commits them to their exact
# committed paths, and opens one pull request recording which commit
# produced them. `build-android`, `build-ios`, and `generate-bindings` run in
# parallel and share nothing but the source commit.
#
# THE OPENED PULL REQUEST CARRIES NO CI OF ITS OWN. It is opened with the
# default `GITHUB_TOKEN`, and a pull request (or push) opened that way does
# not trigger other workflows — so merge-checks.yaml never runs on it
# automatically. This is an accepted, open trade-off (see issue #7's "Open
# questions"), not an oversight: this workflow's own alignment, symbol, and
# slice-count checks already verify the artifacts before they are committed,
# and the next pull request that touches anything else gets full checks as
# normal. Push an empty commit, or open a follow-up pull request, to get
# checks running on this one.

on:
  workflow_dispatch:
    inputs:
      base_branch:
        description: Base branch the opened pull request targets
        required: true
        type: string

concurrency:
  group: espada-engine-artifacts-${{ inputs.base_branch }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-android:
    name: Build Android (.so)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: package.json
          cache: npm

      - name: Setup npm
        run: npm install -g "npm@$(node -p "require('./package.json').engines.npm")"

      - name: Dependency Installation
        run: npm ci

      # Expo's own root-project Gradle plugin (ExpoRootProjectPlugin, in
      # expo-modules-autolinking/android/expo-gradle-plugin) sets
      # `rootProject.ext.ndkVersion` — which every generated
      # `android/*/build.gradle` reads — from an "expoLibs" Gradle version
      # catalog it builds from exactly this file
      # (ExpoAutolinkingSettingsExtension.useExpoVersionCatalog), and nothing
      # in this app overrides that value: verified by reading both plugins'
      # source under node_modules/expo-modules-autolinking/ rather than
      # assumed, after `npx expo prebuild --platform android --no-install`
      # confirmed no plain-text "ndkVersion" ever appears in the generated
      # project itself — the value is resolved by the Gradle plugin at
      # configure time, not written into the generated files as text. Reading
      # it from its actual source, here, is what keeps this job correct
      # across an Expo SDK bump that changes it, rather than hard-coding the
      # version this SDK release happens to pin today (unconfirmable from a
      # primary source during planning — see issue #7).
      - name: Resolve the NDK version this project's Android build uses
        id: ndk_version
        run: |
          set -euo pipefail
          version_file="node_modules/react-native/gradle/libs.versions.toml"
          if [ ! -f "$version_file" ]; then
            echo "::error::$version_file not found — cannot resolve the NDK version react-native pins. Was 'npm ci' run first?"
            exit 1
          fi
          version=$(grep -E '^ndkVersion[[:space:]]*=' "$version_file" | sed -E 's/^ndkVersion[[:space:]]*=[[:space:]]*"([^"]+)".*/\1/')
          if [ -z "$version" ]; then
            echo "::error::Could not parse an ndkVersion entry out of $version_file"
            exit 1
          fi
          echo "Resolved NDK version: $version"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      # Installs the exact numeric NDK package Android's own SDK manager
      # uses (e.g. "27.1.12297006") — the same identifier the resolve step
      # above just read, so there is no letter-release (e.g. "r27d") mapping
      # to get wrong. Lands at $ANDROID_SDK_ROOT/ndk/<version>.
      - name: Install the resolved NDK
        run: sdkmanager --install "ndk;${{ steps.ndk_version.outputs.version }}"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      # cargo-ndk cross-compiles a Rust crate for an Android NDK target,
      # resolving the right clang triple and API level and laying its output
      # out per-ABI (`-o <dir>` producing `<dir>/<abi>/lib<name>.so`) — exactly
      # the jniLibs/<abi>/ shape this crate's binary is committed under.
      # Installed from source (`cargo install`) rather than through
      # taiki-e/install-action: verified against that action's own TOOLS.md
      # that cargo-ndk is not one of its curated tools, so installing it
      # directly is the more certain path rather than relying on its
      # cargo-binstall fallback for a tool it does not list.
      - name: Install cargo-ndk
        run: cargo install cargo-ndk --locked

      - name: Cache Cargo registry and build output
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            modules/espada-engine/lib/target
          key: cargo-android-${{ hashFiles('modules/espada-engine/lib/Cargo.lock') }}

      # -Wl,-z,max-page-size=16384 is passed unconditionally, per the plan:
      # harmless on NDK r28+ (which defaults to 16 KB alignment already) and
      # required on r27 and earlier — which the resolve step above confirms
      # this Expo SDK release still pins. CARGO_TARGET_AARCH64_LINUX_ANDROID_RUSTFLAGS
      # (rather than a committed .cargo/config.toml) is exactly the
      # environment-only mechanism the plan asks for, so nothing here
      # hard-codes an NDK path the way the reference proof of concept did.
      #
      # working-directory is set to the crate itself rather than passing
      # cargo-ndk's own --manifest-path: cargo-ndk 4.1.2 runs `cargo
      # metadata` against the current directory before it ever looks at that
      # flag, so from the repo root it fails with "could not find
      # `Cargo.toml`" regardless of whether --manifest-path is placed before
      # or after `build` — confirmed by trying both. Because the working
      # directory moves, -o is given an absolute path
      # ($GITHUB_WORKSPACE/android-output) so the output still lands at the
      # repo-root android-output/ every later step in this job reads from.
      - name: Cross-compile espada-engine for aarch64-linux-android (arm64-v8a)
        working-directory: modules/espada-engine/lib/espada-engine
        env:
          CARGO_TARGET_AARCH64_LINUX_ANDROID_RUSTFLAGS: "-C link-arg=-Wl,-z,max-page-size=16384"
          RESOLVED_NDK_VERSION: ${{ steps.ndk_version.outputs.version }}
        run: |
          set -euo pipefail
          # android-actions/setup-android exports the SDK root itself; which
          # of the two conventional names it uses is not asserted here from
          # memory, so both are checked and the resolution failing loudly
          # (rather than cargo-ndk failing later with a confusing "NDK not
          # found") is what a wrong assumption here would look like.
          sdk_root="${ANDROID_SDK_ROOT:-${ANDROID_HOME:-}}"
          if [ -z "$sdk_root" ]; then
            echo "::error::Neither ANDROID_SDK_ROOT nor ANDROID_HOME is set after Setup Android SDK."
            exit 1
          fi
          export ANDROID_NDK_HOME="$sdk_root/ndk/$RESOLVED_NDK_VERSION"
          if [ ! -d "$ANDROID_NDK_HOME" ]; then
            echo "::error::Resolved ANDROID_NDK_HOME ($ANDROID_NDK_HOME) does not exist — did the 'Install the resolved NDK' step succeed?"
            exit 1
          fi
          cargo ndk -t arm64-v8a -o "$GITHUB_WORKSPACE/android-output" build --release

      # Google Play has required 16 KB page alignment for native code since
      # 2025-11-01. This reads every PT_LOAD segment's alignment out of the
      # ELF program headers with readelf and fails the job unless the
      # largest one is at least 16384 bytes — which is exactly what the
      # linker flag above controls: on the r27.1.12297006 NDK this job just
      # installed (pre-r28, 4 KB-aligned by default), removing that flag
      # makes every LOAD segment's alignment 0x1000 (4096) and this step
      # fail, which is how this check is known to actually have teeth rather
      # than being unconditionally green.
      - name: Verify the .so is 16 KB page aligned
        run: |
          set -euo pipefail
          so_path="android-output/arm64-v8a/libespada_engine.so"
          if [ ! -f "$so_path" ]; then
            echo "::error::Expected build output at $so_path but it does not exist — the cross-compile step must have failed to produce it."
            exit 1
          fi
          max_align=0
          load_count=0
          while read -r align_hex; do
            load_count=$((load_count + 1))
            align_dec=$((16#${align_hex#0x}))
            if [ "$align_dec" -gt "$max_align" ]; then
              max_align=$align_dec
            fi
          done < <(readelf -lW "$so_path" | awk '$1 == "LOAD" { print $NF }')
          if [ "$load_count" -eq 0 ]; then
            echo "::error::readelf found no LOAD segments in $so_path — cannot verify page alignment."
            exit 1
          fi
          echo "Largest LOAD segment alignment: $max_align bytes (across $load_count LOAD segments)."
          if [ "$max_align" -lt 16384 ]; then
            echo "::error::$so_path is not 16 KB page aligned (largest LOAD segment alignment is $max_align bytes, need at least 16384). Google Play has required 16 KB alignment for native code since 2025-11-01."
            exit 1
          fi
          echo "16 KB page alignment verified."

      # Compares, as sorted sets, the `#[no_mangle] pub extern "C" fn` /
      # `pub unsafe extern "C" fn` names declared in ffi.rs against this
      # build's own dynamic symbols — the same comparison
      # merge-checks.yaml's own rust_checks job runs independently against
      # the already-committed .so on every pull request, and the same one
      # this project's former local rebuild script used to run before this
      # workflow existed. This is a fresh build's own symbols, not the committed
      # binary's, so it exists to keep this job from ever uploading a
      # wrong-symbol artifact — the failure mode this project actually hit
      # once, a committed .so that kept exporting the old `juicio_native_*`
      # names after the C ABI was renamed to `espada_engine_*` — for
      # open-pull-request to then commit unnoticed. That pull request
      # carries no CI of its own (see the header comment above), so this is
      # the only check standing between a wrong-symbol build and a merged
      # binary.
      - name: Verify the built .so's exported C ABI matches ffi.rs
        run: |
          set -euo pipefail
          ffi_rs="modules/espada-engine/lib/espada-engine/src/ffi.rs"
          so_path="android-output/arm64-v8a/libespada_engine.so"

          expected=$(grep -oE '^pub (unsafe )?extern "C" fn [A-Za-z0-9_]+' "$ffi_rs" | awk '{print $NF}' | sort -u)
          actual=$(readelf -sW "$so_path" | awk '$4 == "FUNC" && $5 == "GLOBAL" && $7 != "UND" { print $NF }' | sort -u)

          if [ "$expected" != "$actual" ]; then
            echo "::error::$so_path's exported C ABI symbols do not match $ffi_rs. Refusing to upload a wrong-symbol binary."
            echo "Expected (from ffi.rs):"
            echo "$expected" | sed 's/^/  /'
            echo "Found (in the built .so):"
            echo "$actual" | sed 's/^/  /'
            exit 1
          fi

          echo "Exported C ABI symbols verified:"
          echo "$expected" | sed 's/^/  /'

      - name: Upload the Android binary
        uses: actions/upload-artifact@v4
        with:
          name: android-so
          path: android-output/arm64-v8a/libespada_engine.so
          if-no-files-found: error

  build-ios:
    name: Build iOS (.xcframework)
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Pinned rather than left at the runner image default, for the same
      # reason ios-preview.yaml's own "Select Xcode version" step states and
      # verifies (macos-latest resolves to macOS 26, whose default Xcode a
      # future image rotation can change on its own schedule): reusing that
      # file's own already-recorded verification here rather than
      # re-asserting a fresh one.
      - name: Select Xcode version
        run: |
          sudo xcode-select -s /Applications/Xcode_26.6.app
          xcodebuild -version

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios,aarch64-apple-ios-sim

      - name: Cache Cargo registry and build output
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            modules/espada-engine/lib/target
          key: cargo-ios-${{ hashFiles('modules/espada-engine/lib/Cargo.lock') }}

      # -p espada-engine, not a bare workspace build: the manifest below is a
      # workspace over two crates, and without -p cargo would build every
      # member as a top-level target. espada-internal is still compiled here —
      # it is a path dependency of espada-engine — which is what proves the
      # vendored copy cross-compiles for Apple targets at all.
      - name: Build the aarch64-apple-ios slice (device)
        run: cargo build --release -p espada-engine --target aarch64-apple-ios --manifest-path modules/espada-engine/lib/Cargo.toml

      - name: Build the aarch64-apple-ios-sim slice (Apple-silicon simulator)
        run: cargo build --release -p espada-engine --target aarch64-apple-ios-sim --manifest-path modules/espada-engine/lib/Cargo.toml

      # Verifies each Apple slice's own exported C ABI against
      # modules/espada-engine/lib/espada-engine/src/ffi.rs — the iOS
      # counterpart of the Android job's own exported-symbol check above,
      # carried over from this project's former local rebuild script's own
      # iOS symbol check (that script never had CI to move this into before
      # now). Unlike the Android check, this is a subset check,
      # not an exact-set one: a .a is an intermediate build artifact, not a
      # fully linked binary, and legitimately carries many other global
      # (mangled) Rust symbols a cdylib's dynamic symbol table would not.
      # What must still never be true is this project's own past incident —
      # an expected C ABI name missing or renamed — so this refuses only a
      # *missing* expected symbol, never the presence of others. Mach-O
      # symbol names in nm's own output carry a leading underscore
      # (`_espada_engine_start`), unlike the ELF names the Android job reads
      # directly, which is what the `sed 's/^_//'` below accounts for.
      #
      # This check has never verified a real build before this workflow
      # first runs it: no CI or local session has produced these .a slices
      # or an .xcframework yet (see
      # docs/operations/native-module-artifacts.md's "The Android Binary
      # Exists; the iOS One Does Not"). Its correctness rests on being
      # carried over verbatim from the deleted script's own
      # check_ios_exported_symbols, not on having been exercised here.
      - name: Verify each iOS slice's exported C ABI matches ffi.rs
        run: |
          set -euo pipefail
          ffi_rs="modules/espada-engine/lib/espada-engine/src/ffi.rs"
          expected=$(grep -oE '^pub (unsafe )?extern "C" fn [A-Za-z0-9_]+' "$ffi_rs" | awk '{print $NF}' | sort -u)

          check_slice() {
            local lib_path="$1"
            local found
            found=$(nm -gU "$lib_path" 2>/dev/null | awk '{print $NF}' | sed 's/^_//' | sort -u)

            local missing=""
            while IFS= read -r symbol; do
              [ -z "$symbol" ] && continue
              if ! printf '%s\n' "$found" | grep -qx "$symbol"; then
                missing="$missing $symbol"
              fi
            done <<<"$expected"

            if [ -n "$missing" ]; then
              echo "::error::$lib_path is missing expected C ABI symbol(s) from $ffi_rs:$missing. Refusing to upload a wrong-symbol binary."
              exit 1
            fi
            echo "Exported C ABI symbols verified for $lib_path."
          }

          check_slice "modules/espada-engine/lib/target/aarch64-apple-ios/release/libespada_engine.a"
          check_slice "modules/espada-engine/lib/target/aarch64-apple-ios-sim/release/libespada_engine.a"

      # lipo cannot merge these two static libraries: it keys on CPU
      # architecture alone and both slices are arm64, so an .xcframework is
      # the only mechanism that can carry both — not a preference. No
      # `-headers` argument: the C header this static library implements
      # (modules/espada-engine/cpp/espada_engine.h) is already compiled in
      # directly through the podspec's own `source_files`, not through the
      # xcframework, matching that podspec's own header comment.
      - name: Assemble EspadaEngine.xcframework
        run: |
          set -euo pipefail
          rm -rf EspadaEngine.xcframework
          xcodebuild -create-xcframework \
            -library modules/espada-engine/lib/target/aarch64-apple-ios/release/libespada_engine.a \
            -library modules/espada-engine/lib/target/aarch64-apple-ios-sim/release/libespada_engine.a \
            -output EspadaEngine.xcframework

      - name: Verify the xcframework carries exactly the two expected slices
        run: |
          set -euo pipefail
          plist="EspadaEngine.xcframework/Info.plist"
          if [ ! -f "$plist" ]; then
            echo "::error::Expected $plist to exist after xcodebuild -create-xcframework."
            exit 1
          fi
          identifiers=$(/usr/libexec/PlistBuddy -c "Print :AvailableLibraries" "$plist" 2>/dev/null | grep "LibraryIdentifier" | sed -E 's/.*LibraryIdentifier = //')
          echo "Slices found:"
          echo "$identifiers"
          slice_count=$(printf '%s\n' "$identifiers" | grep -c . || true)
          if [ "$slice_count" -ne 2 ]; then
            echo "::error::Expected exactly 2 slices, found $slice_count: $identifiers"
            exit 1
          fi
          if ! printf '%s\n' "$identifiers" | grep -qx "ios-arm64"; then
            echo "::error::Missing the ios-arm64 (device) slice. Found: $identifiers"
            exit 1
          fi
          if ! printf '%s\n' "$identifiers" | grep -qx "ios-arm64-simulator"; then
            echo "::error::Missing the ios-arm64-simulator slice. Found: $identifiers"
            exit 1
          fi

      - name: Upload the iOS xcframework
        uses: actions/upload-artifact@v4
        with:
          name: ios-xcframework
          path: EspadaEngine.xcframework
          if-no-files-found: error

  generate-bindings:
    name: Generate Nitro Bindings
    runs-on: ubuntu-latest
    # Deliberately no `needs:` here. Nitrogen (`npm run
    # nitrogen:espada-engine`) reads only modules/espada-engine's TypeScript
    # `.nitro.ts` spec and its nitro.json — it invokes no Rust toolchain and
    # produces no binary — and neither Rust cross-compile above reads this
    # job's generated output. There is no dependency between this job and
    # build-android/build-ios in either direction, so it genuinely can run
    # concurrently with them: do not add a `needs:` here to make it "wait
    # for the binaries first," there is nothing for it to wait on.
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: package.json
          cache: npm

      - name: Setup npm
        run: npm install -g "npm@$(node -p "require('./package.json').engines.npm")"

      - name: Dependency Installation
        run: npm ci

      - name: Generate Nitrogen bindings
        run: npm run nitrogen:espada-engine

      - name: Upload the generated bindings
        uses: actions/upload-artifact@v4
        with:
          name: nitrogen-generated
          path: modules/espada-engine/nitrogen/generated/
          if-no-files-found: error

  open-pull-request:
    name: Open Pull Request
    needs: [build-android, build-ios, generate-bindings]
    runs-on: ubuntu-latest
    permissions:
      contents: write # to push the branch this job creates
      pull-requests: write # to open the pull request
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download the Android binary
        uses: actions/download-artifact@v4
        with:
          name: android-so
          path: downloaded/android

      - name: Download the iOS xcframework
        uses: actions/download-artifact@v4
        with:
          name: ios-xcframework
          path: downloaded/ios/EspadaEngine.xcframework

      - name: Download the generated Nitro bindings
        uses: actions/download-artifact@v4
        with:
          name: nitrogen-generated
          path: downloaded/nitrogen-generated

      - name: Commit the binaries and bindings, and open a pull request
        env:
          GH_TOKEN: ${{ github.token }}
          BASE_BRANCH: ${{ inputs.base_branch }}
        run: |
          set -euo pipefail

          source_commit="$(git rev-parse HEAD)"
          branch="add-espada-engine-binaries-${source_commit:0:12}"

          android_dest="modules/espada-engine/android/src/main/jniLibs/arm64-v8a/libespada_engine.so"
          ios_dest="modules/espada-engine/ios/EspadaEngine.xcframework"
          nitrogen_dest="modules/espada-engine/nitrogen/generated"

          mkdir -p "$(dirname "$android_dest")"
          cp downloaded/android/libespada_engine.so "$android_dest"

          rm -rf "$ios_dest"
          cp -R downloaded/ios/EspadaEngine.xcframework "$ios_dest"

          # Replaced wholesale, not merged: a file Nitrogen no longer
          # generates (a spec removed, a renamed method) must actually
          # disappear from the committed tree rather than lingering as a
          # stale leftover a plain copy-over would leave behind.
          rm -rf "$nitrogen_dest"
          cp -R downloaded/nitrogen-generated "$nitrogen_dest"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git checkout -b "$branch"
          # `git add <path>` also stages a removed file's deletion for any
          # tracked path already gone from the working tree (Git 2.0+), not
          # only new and modified content — which is what makes the
          # nitrogen_dest replacement above actually remove a dropped
          # generated file from the index, not just leave it untracked.
          git add "$android_dest" "$ios_dest" "$nitrogen_dest"

          if git diff --cached --quiet; then
            echo "::error::Nothing to commit — the built binaries and generated bindings are byte-identical to what is already committed at these paths. Refusing to open an empty pull request."
            exit 1
          fi

          git commit -m "chore(native): commit espada-engine binaries and generated bindings built from ${source_commit}"
          git push origin "$branch"

          # Built as a plain variable rather than a heredoc: a heredoc's
          # closing delimiter must be flush against the left margin, which a
          # YAML block-scalar `run: |` step (indented to match the rest of
          # this step) cannot give it without ending the block early.
          pr_body=$(printf '%s\n' \
            "Commits the Android \`.so\`, the iOS \`.xcframework\`, and the" \
            "generated Nitro bindings this workflow just built and generated from" \
            "\`modules/espada-engine\` at commit \`${source_commit}\`." \
            "" \
            "- \`${android_dest}\`" \
            "- \`${ios_dest}\`" \
            "- \`${nitrogen_dest}/\`" \
            "" \
            "Opened with the default \`GITHUB_TOKEN\`, so this pull request does not" \
            "itself trigger other workflows (including merge-checks) — an accepted," \
            "open trade-off recorded in the plan behind this change (issue #7). Push" \
            "an empty commit, or open a follow-up pull request, to get checks" \
            "running on it.")

          gh pr create \
            --base "$BASE_BRANCH" \
            --head "$branch" \
            --title "chore(native): commit espada-engine binaries and bindings (${source_commit:0:12})" \
            --body "$pr_body"
